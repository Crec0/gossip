version: '3'

services:
    ip-pls:
        build: .
        container_name: ip-pls
        restart: always

        environment:
            - IP_SERVICE_PORT=10059

#        Or .env
#        env_file:
#            -   ".env"

        ports:
            - '${IP_SERVICE_PORT}:${IP_SERVICE_PORT}'
